/**
 * 稍后阅读 — 把感兴趣的页面存入阅读列表，统一管理和回顾
 *
 * 适用场景：
 * - 用户说“这个页面不错，先存起来以后看”、“帮我看看我的阅读列表里有什么”
 * - 浏览时遇到想读但暂时没时间的内容
 */
import type { Skill } from '@/shared/types';

export const readingListSkill: Omit<Skill, 'id' | 'createdAt' | 'updatedAt' | 'enabled'> = {
  name: 'reading-list',
  description: '当用户想保存当前页面留待以后阅读，或想查看/管理已保存的阅读列表时使用',
  prompt: `## 阅读列表技能

你已激活“阅读列表”技能。帮用户管理稍后阅读的内容。

### 操作 A：添加到阅读列表
当用户想保存当前页面时：

1. 先确认要保存的范围：
   - **当前标签页**（默认）
   - **当前窗口所有标签页**
   - **用户选中的几个标签页**

2. 使用 \`page_getMetadata\` 获取每个页面元数据（标题、描述）

3. 使用 \`bookmarks_getTree\` 检查是否已有“阅读列表”书签文件夹
   - 没有则用 \`bookmarks_create\` 在书签栏创建“📖 阅读列表”文件夹

4. 用 \`bookmarks_create\` 将页面加入阅读列表，标题格式：
   - \`YYYY-MM-DD {页面标题}\`
   - 如果标题太长（>50字符），截断并保留关键信息

### 操作 B：查看阅读列表
当用户想查看已保存的阅读列表时：

1. 使用 \`bookmarks_search\` 搜索“阅读列表”文件夹下的内容
2. 按添加日期分组展示：
\`\`\`
📖 阅读列表（共 12 篇）：
  2026-07-11:
    - React 18 并发模式详解 (新增)
    - CSS Container Queries 实战
  2026-07-08:
    - Rust 异步编程入门
    ...
\`\`\`

### 操作 C：开始阅读
当用户说“开始阅读”时：

1. 展示在线阅读列表
2. 询问要读哪一篇（最近添加的优先推荐）
3. 用 \`tabs_create\` 在新标签页打开该文章
4. 打开后，询问用户：
   - **已读完** → \`bookmarks_delete\` 从列表中移除
   - **还没读完** → 保留，下次继续
   - **加入永久书签** → \`bookmarks_update\` 移到其他书签文件夹

### 操作 D：清理阅读列表
当用户想清理时：

1. 展示阅读列表
2. 列出超过 30 天未读的文章（可能已失去兴趣）
3. 询问：删除 / 移到永久书签 / 暂时保留

### 重要提醒
- 阅读列表不同于普通书签，是一个待消费的队列
- 添加时自动记录日期，方便用户感知积压
- 已读的文章不要直接删，给用户一个"存档"选项`,
  source: 'builtin:cross',
  resources: [],
};
