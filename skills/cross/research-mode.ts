/**
 * 研究模式 — 将分散的研究标签页归拢到一个窗口，按主题/域名分组，方便集中浏览
 *
 * 适用场景：
 * - 用户说“帮我把这些关于 XXX 的页面整理到一起”
 * - 打开了多个相关的文档/资料/论文，需要集中查看
 */
import type { Skill } from '@/shared/types';

export const researchModeSkill: Omit<Skill, 'id' | 'createdAt' | 'updatedAt' | 'enabled'> = {
  name: 'research-mode',
  description: '当用户打开了很多关于同一主题的页面做调研，想把它们归拢到同一个窗口集中浏览时使用',
  prompt: `## 研究模式技能

你已激活“研究模式”技能。帮用户把分散的调研标签页组织成一个专注的研究工作区。

### 步骤 1：确认研究主题
询问用户：
- 本次研究的主题是什么？（如“React 状态管理方案调研”、“Kubernetes 部署方案对比”）
- 要归拢哪些标签页？
  - **当前窗口全部标签页**
  - **用户指定部分标签页**（可以描述匹配规则，如“所有 GitHub 上的 issue 页面”、“所有 docs.* 的文档”）
  - **当前窗口 + 其他窗口的相关标签页**

### 步骤 2：收集标签页
使用 \`tabs_query\` 收集目标标签页，排除：
- 邮件、聊天、日历等非研究页面
- 空白新标签页
- 用户明确要求排除的域名

### 步骤 3：创建研究窗口
1. 使用 \`windows_create\` 创建一个新窗口（不激活，让用户决定何时切换）
2. 使用 \`tabs_move\` 将所有研究相关标签页移入该窗口
3. 使用 \`tabs_group\` 按域名/子主题创建标签组：
   - 同一域名（如 react.dev、github.com/facebook/react）归为一个标签组
   - 不同域名但同主题的可以归入同一个标签组
   - 组名体现子主题，如“官方文档”、“社区讨论”、“参考实现”

### 步骤 4：组织标签页顺序
调整窗口内标签页顺序，按研究逻辑排列：
1. 概览/总览类页面（如 README、官方首页）放最前面
2. 核心技术文档紧随其后
3. 参考案例/对比分析
4. 社区讨论/issue 跟踪

### 步骤 5：创建研究书签
在书签栏创建“研究 - {主题名}”文件夹，将所有标签页 URL 加入书签，方便日后快速找回。

### 步骤 6：汇报
\`\`\`
研究工作区已就绪：
- 主题：React 状态管理方案调研
- 新窗口包含 12 个标签页
- 创建了 4 个标签组：官方文档 / Zustand / Jotai / 社区讨论
- 书签已保存到“研究 - React 状态管理”文件夹
\`\`\`

### 步骤 7：后续归档（用户主动触发时）
当用户说“归档这个研究”时：
1. 使用 \`sessions_save\` 保存当前窗口会话快照
2. 可选：使用 \`page_getMarkdown\` 逐页抓取内容并保存到 storage
3. 使用 \`page_viewMarkdown\` 生成研究汇总页面
4. 询问用户是否关闭研究窗口

### 重要提醒
- 不要直接在新窗口激活（避免打断用户当前工作）
- 移动标签页前列出清单，等待用户确认
- 标签组命名要简短有意义，能反映子主题
- 如果标签页已经在其他窗口中且所属窗口只有一个标签页，移空后该窗口会自动关闭`,
  source: 'builtin:cross',
  resources: [],
};
