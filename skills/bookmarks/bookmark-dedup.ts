/**
 * 书签去重 — 扫描并清理重复书签
 *
 * 适用场景：
 * - 用户说“我的书签有很多重复的，帮我清理一下”
 * - 导入书签后出现大量重复
 */
import type { Skill } from '@/shared/types';

export const bookmarkDedupSkill: Omit<Skill, 'id' | 'createdAt' | 'updatedAt' | 'enabled'> = {
  name: 'bookmark-dedup',
  description: '当用户发现书签有很多重复、想清理重复书签时使用',
  prompt: `## 书签去重技能

你已激活“书签去重”技能。请按以下步骤清理重复书签：

### 步骤 1：获取完整书签树
使用 \`bookmarks_getTree\` 获取所有书签（包括文件夹结构）。忽略文件夹节点，只关注叶子节点（url 不为空的节点）。

### 步骤 2：检测重复
按以下维度检测重复：
- **完全重复**：URL 完全相同
- **相似 URL**：忽略 URL 末尾的 slash、www 前缀、query 参数（?utm_source= 等追踪参数）后相同
- **同名书签**：title 相同或高度相似（忽略特殊字符、多余空格）

### 步骤 3：生成去重方案
对每组重复，选出保留哪个。保留原则（优先级从高到低）：
1. 在书签栏（bookmark bar）中的优先保留
2. 创建时间较早的优先保留
3. 有自定义名称的优先于仅有 URL 的

列出将要删除的书签清单（名称、URL、所在文件夹），等待用户确认。

### 步骤 4：执行删除
用户确认后，使用 \`bookmarks_delete\` 逐一删除重复书签。
注意：删除操作会触发确认弹窗。

### 步骤 5：汇报结果
向用户汇报：
1. 共扫描了多少书签
2. 发现多少组重复（每组几个重复）
3. 删除了多少个重复书签
4. 剩余书签数量

### 重要提醒
- 不要删除文件夹节点
- 不同文件夹中的书签也可能是重复的，不要忽略
- 删除操作需要用户确认
- 如果某组重复的书签分散在不同文件夹中，向用户说明来源并把用户的选择记为最终方案`,
  source: 'builtin:bookmarks',
  resources: [],
};
