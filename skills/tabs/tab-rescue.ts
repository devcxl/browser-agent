/**
 * 标签页过载救援 — 超过阈值时自动去重 → 休眠 → 分组 → 归档
 *
 * 适用场景：
 * - 用户说“我打开太多标签页了，帮我救一下”
 * - 打开超过 50 个标签页时主动触发整理
 */
import type { Skill } from '@/shared/types';

export const tabRescueSkill: Omit<Skill, 'id' | 'createdAt' | 'updatedAt' | 'enabled'> = {
  name: 'tab-rescue',
  description: '当用户打开标签页过多（超过40个）、浏览器变得混乱难以管理时使用',
  prompt: `## 标签页过载救援技能

你已激活“标签页过载救援”技能。当用户打开标签页过多（建议阈值 40+）时，按以下优先级抢救：

### 步骤 1：全局扫描
使用 \`tabs_query\` 获取所有标签页。统计：
- 总数
- 各窗口分布
- pinned 标签页数（不操作）
- 重复 URL 数量

### 步骤 2：去重 — 最高优先级
找出所有 URL 重复的标签页。每组只保留 1 个，优先保留：
1. 有表单填写状态的（从 title 判断，如未保存的草稿）
2. 在当前活跃窗口中的
3. 最近访问的
关闭其余重复标签页（使用 \`tabs_remove\`，会触发确认）。

### 步骤 3：休眠闲置标签页 — 中等优先级
对最后访问时间超过 12 小时的标签页：
1. 使用 \`bookmarks_create\` 将它们保存到“休眠标签页”书签文件夹
2. 使用 \`tabs_remove\` 关闭
建议一次性处理不超过 20 个，分批确认。

### 步骤 4：分组整理剩余标签页 — 低优先级
对剩余标签页使用 \`tabs_group\` 按域名分组，减少视觉混乱：
- 同域名 ≥ 2 个即可成组
- 组名简短：GitHub、文档、视频、AI 工具 等

### 步骤 5：汇报
| 项目 | 数量 |
|------|------|
| 原始标签页数 | N |
| 关闭的重复标签页 | N |
| 归档的闲置标签页 | N |
| 剩余活跃标签页 | N |
| 创建的标签组 | N |

### 重要提醒
- pinned 标签页不操作
- 有播放中媒体（音频/视频）的标签页不关闭
- 每步操作都需要用户确认
- 如果用户只想要部分操作（比如只去重不安抚），按需调整`,
  source: 'builtin:tabs',
  resources: [],
};
