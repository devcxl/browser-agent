/**
 * 工作区快照 — 保存/恢复当前窗口的所有标签页为一个工作区
 *
 * 适用场景：
 * - 用户说“帮我保存当前这些标签页，下次恢复”
 * - 在不同工作任务之间切换
 */
import type { Skill } from '@/shared/types';

export const tabWorkspaceSkill: Omit<Skill, 'id' | 'createdAt' | 'updatedAt' | 'enabled'> = {
  name: 'tab-workspace',
  description: '当用户想保存当前工作状态以便后续恢复，或在不同工作任务间切换时使用',
  prompt: `## 工作区快照技能

你已激活“工作区快照”技能。请按以下步骤操作：

### 步骤 1：确认用户意图
先确认用户想要：
- **保存**当前工作区？还是
- **恢复**某个已保存的工作区？还是
- **列出**所有已保存的工作区？

### 步骤 2A：保存工作区
1. 使用 \`tabs_query\` 获取当前窗口所有标签页（需要 url 和 title）
2. 使用 \`sessions_save\` 保存当前会话，命名采用“日期 + 用途”格式
   - 例如：“2026-07-11 前端开发”、“工作区：React 重构”
3. 同时创建一个书签文件夹记录标签页快照：
   - 使用 \`bookmarks_create\` 在“工作区”书签文件夹下创建子文件夹
   - 把当前所有标签页 URL 添加为书签，方便即使 sessions API 不完整也能恢复
4. 汇报：保存了多少个标签页，工作区名称是什么

### 步骤 2B：恢复工作区
1. 先问用户要在**当前窗口打开**还是**新窗口打开**
2. 使用 \`sessions_restore\` 恢复指定会话到目标窗口
3. 汇报恢复了多少个标签页

### 步骤 2C：列出工作区
1. 使用 \`sessions_list\` 列出所有已保存的会话
2. 同时使用 \`bookmarks_search\` 搜索“工作区”书签文件夹
3. 展示列表，格式：“名称 — 标签页数 — 保存时间”

### 重要提醒
- 保存前可以问用户是否要排除一些不需要的标签页
- 工作区名称尽量简短有意义，可由用户指定或由 AI 根据标签页内容智能命名
- sessions 可能只包含最近关闭的窗口，结合书签方案可以作为补充`,
  source: 'builtin:tabs',
  resources: [],
};
