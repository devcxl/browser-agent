/**
 * 智能分组 — 按域名/主题对标签页进行分组整理
 *
 * 适用场景：
 * - 用户说“帮我把标签页按网站分组”
 * - 打开了多个项目的相关页面，想按主题分类
 */
import type { Skill } from '@/shared/types';

export const tabSorterSkill: Omit<Skill, 'id' | 'createdAt' | 'updatedAt' | 'enabled'> = {
  name: 'tab-sorter',
  description: '当用户想把标签页按网站分类整理、创建标签组时使用',
  prompt: `## 智能标签分组技能

你已激活“智能标签分组”技能。请按以下步骤整理用户的标签页分组：

### 步骤 1：获取全部标签页
使用 \`tabs_query\` 获取当前窗口中所有标签页。

### 步骤 2：分析标签页域名分布
从每个标签页的 \`url\` 中提取域名（hostname），统计每个域名下有多少标签页。

### 步骤 3：决策分组策略
按以下规则决定如何分组：
- **单域名 ≥ 3 个标签页**：独立成为一个分组
- **单域名 1-2 个标签页**：如果该域名属于某个已知大类（如“AI 工具”、“社交媒体”、“开发工具”、“视频”等），归入对应大类分组；否则归入“其他”
- **同一产品线**（如 github.com + github.io）可以合并为一个分组

### 步骤 4：创建标签组
使用 \`tabs_group\` 为每组标签页创建一个标签组。为每个组设置：
- 一个有意义的标题（如“GitHub”、“文档”、“AI 工具”）
- 一个颜色（每个组不同颜色）

示例调用：
\`\`\`json
{
  "tabIds": [123, 456, 789],
  "groupId": -1,
  "createProperties": { "title": "GitHub", "color": "blue" }
}
\`\`\`

### 步骤 5：处理单标签页的域名
对于只有 1 个标签页的域名，可以建议用户是否要关闭，或者归入“待处理”分组。

### 步骤 6：汇报结果
向用户汇报：
1. 总共多少个标签页
2. 创建了多少个标签组
3. 每个标签组包含哪些网站
4. 是否有未分组的标签页

### 重要提醒
- 标签组名称要简短、有意义
- 不要对已经分组的标签页重新分组（检查 \`groupId\` 字段）
- 如果有标签页没有 \`url\`（如新标签页），放入“空白页”分组`,
  source: 'builtin:tabs',
  resources: [],
};
