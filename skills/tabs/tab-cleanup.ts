/**
 * 标签页大扫除 — 关闭重复标签页 + 归档长期未访问标签页
 *
 * 适用场景：
 * - 用户说“标签页太多了帮我清理一下”
 * - 打开了几十个标签页想快速精简
 */
import type { Skill } from '@/shared/types';

export const tabCleanupSkill: Omit<Skill, 'id' | 'createdAt' | 'updatedAt' | 'enabled'> = {
  name: 'tab-cleanup',
  description: '当用户抱怨标签页太多、太乱，或想清理重复和长时间未使用的标签页时使用',
  prompt: `## 标签页大扫除技能

你已激活“标签页大扫除”技能。请按以下步骤整理用户的标签页：

### 步骤 1：获取全部标签页
使用 \`tabs_query\` 获取所有打开的标签页，关注以下字段：
- \`url\` — 标签页地址
- \`title\` — 标签页标题
- \`id\` — 标签页 ID
- \`lastAccessed\` — 最后访问时间（时间戳 ms）

### 步骤 2：查找重复标签页
找出所有 URL 重复的标签页，每组重复只保留一个（优先保留最早打开的或正活跃的），其余标记为可关闭。

### 步骤 3：识别闲置标签页
找出最后访问时间超过 24 小时的标签页。这些属于“闲置标签页”。

### 步骤 4：归档闲置标签页
对闲置标签页，先使用 \`bookmarks_create\` 将其加入书签（建议放在“归档标签页”文件夹下），再使用 \`tabs_remove\` 关闭。
注意：关闭标签页前会触发确认弹窗，你需要向用户说明将要关闭哪些标签页。

### 步骤 5：关闭重复标签页
使用 \`tabs_remove\` 分批关闭重复标签页。
注意：同样会触发确认弹窗。

### 步骤 6：汇报结果
向用户汇报：
1. 总共扫描了多少标签页
2. 找到多少组重复标签页，关闭了多少个
3. 归档了多少个闲置标签页
4. 当前剩余标签页数量

### 重要提醒
- 关闭标签页和删除操作每次都需要用户确认，不要跳过
- 不要关闭 pinned 标签页
- 不要关闭有未保存表单内容的标签页（如果有办法判断的话）
- 用户可能想保留某些标签页，先列出将要操作的清单，等用户确认后再批量执行`,
  source: 'builtin:tabs',
  resources: [],
};
