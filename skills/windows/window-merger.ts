/**
 * 窗口合并 — 将所有窗口的标签页合并到一个窗口
 *
 * 适用场景：
 * - 用户说“帮我把所有窗口的标签页合并到一起”
 * - 开了 5-6 个窗口，想归拢
 */
import type { Skill } from '@/shared/types';

export const windowMergerSkill: Omit<Skill, 'id' | 'createdAt' | 'updatedAt' | 'enabled'> = {
  name: 'window-merger',
  description: '当用户打开了多个浏览器窗口，想把所有标签页合并到一个窗口统一管理时使用',
  prompt: `## 窗口合并技能

你已激活“窗口合并”技能。将所有分散的窗口标签页合并到主窗口。

### 步骤 1：获取所有窗口和标签页
1. 使用 \`windows_getAll\` 获取所有窗口及每个窗口的标签页
2. 向用户展示当前窗口分布：
\`\`\`
窗口 1 (当前): 12 个标签页 — GitHub, StackOverflow, ...
窗口 2: 5 个标签页 — YouTube, Bilibili, ...
窗口 3: 3 个标签页 — Gmail, Calendar, ...
\`\`\`

### 步骤 2：选择合并策略
让用户选择：
- **全部合并到当前窗口**：其他窗口关闭，标签页全部移入
- **合并到新窗口**：创建一个新窗口，所有标签页移入
- **合并到指定窗口**：用户指定哪个窗口作为目标
- **仅合并部分窗口**：用户勾选要合并的窗口

### 步骤 3：执行合并
1. 对每个源窗口的标签页：
   - 使用 \`tabs_move\` 将标签页移动到目标窗口
   - 记录原始窗口来源
2. 源窗口标签页移空后，自动关闭源窗口（使用 \`windows_remove\`）
3. 在目标窗口中，使用 \`tabs_group\` 按原始窗口来源创建标签组：
   - 组名如“从窗口 2 移入”
   - 不同颜色区分

### 步骤 4：汇报
\`\`\`
合并完成：
- 原有 3 个窗口 → 现在 1 个窗口
- 共合并 20 个标签页
- 关闭了 2 个空窗口
- 创建了 3 个标签组（按原始窗口分组）
\`\`\`

### 重要提醒
- pinned 标签页保持在所有标签页之前
- 注意 Chrome 窗口数量限制
- 如果源窗口有正在播放媒体的标签页，提醒用户
- 移动标签页时保持原有顺序`,
  source: 'builtin:windows',
  resources: [],
};
